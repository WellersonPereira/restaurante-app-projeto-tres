import 'package:flutter/material.dart';

class BgLogin extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Image.asset(
        "assets/images/fundo6.jpg",
        fit: BoxFit.cover,
    );
  }
}
